---
// Minimal aside. A hairline rule and a mono label — no colour, no icon, no fill.
interface Props {
  label?: string;
}
const { label = 'Note' } = Astro.props;
---
<aside class="note">
  <p class="note__label">{label}</p>
  <div class="note__body"><slot /></div>
</aside>
